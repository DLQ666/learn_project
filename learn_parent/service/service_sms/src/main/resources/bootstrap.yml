spring:
  application:
    name: service-sms
  profiles:
    active: prod # 环境标识，test、prod 等
  cloud:
    nacos:
      config:
        server-addr: gitdlq.top:8848 #nacos中心地址
        file-extension: yaml # 配置文件格式，如果是properties文件则不用配置此项
        namespace: 171098e7-e0e0-4cf7-b482-b7ba15106a0a
